version: '3'
services:
  automata-api:
    restart: always
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - '8080:8080'

    networks:
      - redesophy
    volumes:
      - ./cache-docker:/app/cache
  

networks:
  redesophy:
    driver: bridge